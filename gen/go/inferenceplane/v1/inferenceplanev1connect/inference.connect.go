// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: inferenceplane/v1/inference.proto

package inferenceplanev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/inference-book/inference-plane/gen/go/inferenceplane/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// InferenceServiceName is the fully-qualified name of the InferenceService service.
	InferenceServiceName = "inferenceplane.v1.InferenceService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// InferenceServiceCompleteProcedure is the fully-qualified name of the InferenceService's Complete
	// RPC.
	InferenceServiceCompleteProcedure = "/inferenceplane.v1.InferenceService/Complete"
	// InferenceServiceChatCompleteProcedure is the fully-qualified name of the InferenceService's
	// ChatComplete RPC.
	InferenceServiceChatCompleteProcedure = "/inferenceplane.v1.InferenceService/ChatComplete"
)

// InferenceServiceClient is a client for the inferenceplane.v1.InferenceService service.
type InferenceServiceClient interface {
	// Complete runs plain prompt-completion inference.
	// Hand-mapped to OpenAI's POST /v1/completions in internal/web/server/.
	Complete(context.Context, *connect.Request[v1.CompleteRequest]) (*connect.Response[v1.CompleteResponse], error)
	// ChatComplete runs chat-style inference over a list of messages.
	// Hand-mapped to OpenAI's POST /v1/chat/completions in internal/web/server/.
	ChatComplete(context.Context, *connect.Request[v1.ChatCompleteRequest]) (*connect.Response[v1.ChatCompleteResponse], error)
}

// NewInferenceServiceClient constructs a client for the inferenceplane.v1.InferenceService service.
// By default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped
// responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewInferenceServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) InferenceServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	inferenceServiceMethods := v1.File_inferenceplane_v1_inference_proto.Services().ByName("InferenceService").Methods()
	return &inferenceServiceClient{
		complete: connect.NewClient[v1.CompleteRequest, v1.CompleteResponse](
			httpClient,
			baseURL+InferenceServiceCompleteProcedure,
			connect.WithSchema(inferenceServiceMethods.ByName("Complete")),
			connect.WithClientOptions(opts...),
		),
		chatComplete: connect.NewClient[v1.ChatCompleteRequest, v1.ChatCompleteResponse](
			httpClient,
			baseURL+InferenceServiceChatCompleteProcedure,
			connect.WithSchema(inferenceServiceMethods.ByName("ChatComplete")),
			connect.WithClientOptions(opts...),
		),
	}
}

// inferenceServiceClient implements InferenceServiceClient.
type inferenceServiceClient struct {
	complete     *connect.Client[v1.CompleteRequest, v1.CompleteResponse]
	chatComplete *connect.Client[v1.ChatCompleteRequest, v1.ChatCompleteResponse]
}

// Complete calls inferenceplane.v1.InferenceService.Complete.
func (c *inferenceServiceClient) Complete(ctx context.Context, req *connect.Request[v1.CompleteRequest]) (*connect.Response[v1.CompleteResponse], error) {
	return c.complete.CallUnary(ctx, req)
}

// ChatComplete calls inferenceplane.v1.InferenceService.ChatComplete.
func (c *inferenceServiceClient) ChatComplete(ctx context.Context, req *connect.Request[v1.ChatCompleteRequest]) (*connect.Response[v1.ChatCompleteResponse], error) {
	return c.chatComplete.CallUnary(ctx, req)
}

// InferenceServiceHandler is an implementation of the inferenceplane.v1.InferenceService service.
type InferenceServiceHandler interface {
	// Complete runs plain prompt-completion inference.
	// Hand-mapped to OpenAI's POST /v1/completions in internal/web/server/.
	Complete(context.Context, *connect.Request[v1.CompleteRequest]) (*connect.Response[v1.CompleteResponse], error)
	// ChatComplete runs chat-style inference over a list of messages.
	// Hand-mapped to OpenAI's POST /v1/chat/completions in internal/web/server/.
	ChatComplete(context.Context, *connect.Request[v1.ChatCompleteRequest]) (*connect.Response[v1.ChatCompleteResponse], error)
}

// NewInferenceServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewInferenceServiceHandler(svc InferenceServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	inferenceServiceMethods := v1.File_inferenceplane_v1_inference_proto.Services().ByName("InferenceService").Methods()
	inferenceServiceCompleteHandler := connect.NewUnaryHandler(
		InferenceServiceCompleteProcedure,
		svc.Complete,
		connect.WithSchema(inferenceServiceMethods.ByName("Complete")),
		connect.WithHandlerOptions(opts...),
	)
	inferenceServiceChatCompleteHandler := connect.NewUnaryHandler(
		InferenceServiceChatCompleteProcedure,
		svc.ChatComplete,
		connect.WithSchema(inferenceServiceMethods.ByName("ChatComplete")),
		connect.WithHandlerOptions(opts...),
	)
	return "/inferenceplane.v1.InferenceService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case InferenceServiceCompleteProcedure:
			inferenceServiceCompleteHandler.ServeHTTP(w, r)
		case InferenceServiceChatCompleteProcedure:
			inferenceServiceChatCompleteHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedInferenceServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedInferenceServiceHandler struct{}

func (UnimplementedInferenceServiceHandler) Complete(context.Context, *connect.Request[v1.CompleteRequest]) (*connect.Response[v1.CompleteResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("inferenceplane.v1.InferenceService.Complete is not implemented"))
}

func (UnimplementedInferenceServiceHandler) ChatComplete(context.Context, *connect.Request[v1.ChatCompleteRequest]) (*connect.Response[v1.ChatCompleteResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("inferenceplane.v1.InferenceService.ChatComplete is not implemented"))
}
