# Demo 07 is a self-contained shell walkthrough -- run.sh starts its own
# mock engines + serve, so there's no separate `make serve` or DEPLOY_ID.
# Needs a prebuilt bin/iplane (run `make build` at the repo root first).

demo: ## Run the prefix-affinity walkthrough (07a / 07b / 07c, GPU-free)
	bash run.sh

.PHONY: demo
