DEMO_BIN := router-in-path
include ../common/walkthrough.mk
include ../common/serve.mk

# 04 ships no per-demo config.yaml; the global deploy/config.yaml is
# already the Beat 1 narrative (router.queue.servicers: 0, direct-
# forward path). `make serve` here picks up the global fallback.
