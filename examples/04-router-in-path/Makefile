DEMO_BIN := router-in-path
include ../common/walkthrough.mk

# 04 drives a running `iplane serve` + RunPod; there's no standalone
# serve target here -- bring up the daemon with `iplane serve` (or via
# demo 03's `make serve`) before `make demo`.
